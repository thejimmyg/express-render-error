{
  "name": "express-render-error",
  "version": "0.1.1",
  "description": "Set up an express server with error handlers, signal handlers and common environment variable support.",
  "scripts": {
    "docker:build": "docker build . -t thejimmyg/express-render-error:0.1.1",
    "docker:push": "docker push thejimmyg/express-render-error:0.1.1",
    "docker:build:test": "docker build . -t thejimmyg/express-render-error:test",
    "docker:push:test": "docker push thejimmyg/express-render-error:test",
    "docker:run": "docker run -e SCRIPT_NAME='' -e DEBUG'=express-render-error:server,express-render-error' -e PORT=80 --init -p 80:80 thejimmyg/express-render-error:0.1.1",
    "fix": "standard --fix index.js example/*.js bin/*.js lib/*.js"
  },
  "author": "JimmyG",
  "license": "MIT",
  "dependencies": {
    "debug": "^4.1.1"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:thejimmyg/express-render-error.git"
  },
  "devDependencies": {
    "standard": "^12.0.1"
  }
}
